import styled from "styled-components";
import { Layout, Menu} from 'antd';

export const NavUp = styled(Menu)`
color:#001529;
background-color: #F0F0F0;

`;
export const LayoutBox = styled(Layout)`
background-color: white;

`;
